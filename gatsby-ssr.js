import wrapWithProvider from 'src/utilities/wrap-with-provider'

export const wrapRootElement = wrapWithProvider

// export const wrapPageElement = ({ element, props }) => {
//    return <Chrome { ...props }>{ element }</Chrome>
// }
